# All scripts in this folder use only the Python standard library
# (urllib, json, re, datetime) so there is nothing to install.
# This file exists so the workflow and any local run instructions
# have a stable `pip install -r scripts/requirements.txt` command
# to grow into if a dependency is ever added later.
